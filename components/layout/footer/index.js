import styles from '@/styles/layout.module.scss';

const Footer = () => {
	return (
		<footer className={styles.footer}>
			<p className={styles.copyright}>copyright c Enzwel</p>
		</footer>
	);
};

export default Footer;
